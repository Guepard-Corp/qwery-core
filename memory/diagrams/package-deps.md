# Package dependency graph

> Auto-generated by `dependency-cruiser` — do not hand-edit.
>
> Refresh with: `bun x depcruise --include-only "^src" --output-type mermaid src`
>
> Will become a real package graph after the monorepo restructure.

```mermaid
graph LR
  app["src/app.tsx"]
  main["src/main.tsx"]
  agent_loop["src/agent/loop.ts"]
  agent_tools["src/agent/tools.ts"]
  duckdb["src/adapters/duckdb.ts"]
  llm["src/adapters/llm.ts"]
  provider["src/domain/provider.ts"]
  slash["src/domain/slash-commands.ts"]
  config["src/lib/config.ts"]
  logger["src/lib/logger.ts"]
  validator["src/lib/aggregate-validator.ts"]
  mustache["src/lib/mustache-helpers.ts"]
  tui_chat["src/tui/chat-view.tsx"]
  tui_input["src/tui/input-bar.tsx"]
  tui_models["src/tui/models-overlay.tsx"]
  tui_results["src/tui/results-view.tsx"]
  tui_schema["src/tui/schema-view.tsx"]
  tui_sql["src/tui/sql-highlight.tsx"]
  tui_table["src/tui/table.tsx"]
  tui_tabs["src/tui/tabs.tsx"]
  tui_toolcall["src/tui/tool-call.tsx"]

  main --> app
  app --> agent_loop
  app --> agent_tools
  app --> logger
  app --> tui_chat
  app --> tui_input
  app --> tui_models
  app --> tui_results
  app --> tui_schema
  app --> tui_tabs
  app --> duckdb
  agent_loop --> llm
  agent_loop --> logger
  agent_loop --> agent_tools
  agent_tools --> duckdb
  agent_tools --> validator
  agent_tools --> mustache
  llm --> provider
  llm --> config
  tui_chat --> tui_toolcall
  tui_chat --> tui_table
  tui_chat --> mustache
  tui_results --> tui_table
  tui_results --> tui_schema
  tui_results --> tui_sql
  tui_input --> slash
  tui_input --> logger
  tui_models --> provider
  tui_models --> config
  tui_models --> logger
```
