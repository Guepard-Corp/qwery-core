{
  "name": "@qwery/extension-json-online",
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./driver": "./src/driver.ts",
    "./schema": "./src/schema.ts"
  },
  "contributes": {
    "datasources": [
      {
        "id": "json-online",
        "name": "JSON (Online)",
        "description": "Query a public JSON file by http(s) URL via DuckDB.",
        "tags": ["Files"],
        "icon": "📋",
        "drivers": ["json-online.duckdb"]
      }
    ],
    "drivers": [
      {
        "id": "json-online.duckdb",
        "name": "DuckDB (Node)",
        "description": "DuckDB-backed driver for online JSON files",
        "runtime": "node",
        "entry": "./src/driver.ts"
      }
    ]
  },
  "dependencies": {
    "@qwery/domain": "workspace:*",
    "@qwery/extension-sdk": "workspace:*",
    "zod": "^4.4.3"
  }
}
