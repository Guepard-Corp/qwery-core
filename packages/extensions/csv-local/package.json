{
  "name": "@qwery/extension-csv-local",
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./driver": "./src/driver.ts",
    "./schema": "./src/schema.ts"
  },
  "contributes": {
    "datasources": [
      {
        "id": "csv-local",
        "name": "CSV (local file)",
        "description": "Read a local CSV file from disk via DuckDB.",
        "tags": ["Files"],
        "icon": "📄",
        "drivers": ["csv-local.duckdb"]
      }
    ],
    "drivers": [
      {
        "id": "csv-local.duckdb",
        "name": "DuckDB (Node)",
        "description": "DuckDB-backed driver for local CSV files",
        "runtime": "node",
        "entry": "./src/driver.ts"
      }
    ]
  },
  "dependencies": {
    "@qwery/domain": "workspace:*",
    "@qwery/extension-sdk": "workspace:*",
    "zod": "^4.4.3"
  }
}
